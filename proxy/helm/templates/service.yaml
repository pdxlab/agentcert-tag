apiVersion: v1
kind: Service
metadata: { name: {{ .Release.Name }}-mcp-trust-proxy }
spec:
  type: {{ .Values.service.type }}
  selector: { app: {{ .Release.Name }}-mcp-trust-proxy }
  ports: [{ port: {{ .Values.service.port }}, targetPort: {{ .Values.service.port }} }]
