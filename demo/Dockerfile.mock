# Self-contained mock TrustModel backend (CA + reputation + transparency).
FROM python:3.12-slim
WORKDIR /app
COPY demo/mock_trustmodel.py /app/mock_trustmodel.py
RUN pip install --no-cache-dir "cryptography>=42"
EXPOSE 9000
CMD ["python", "/app/mock_trustmodel.py"]
